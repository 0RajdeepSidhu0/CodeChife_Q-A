#include <stdio.h>

int main(void) {
	int t;
	scanf("%d", &t);
	for(int i=0; i<t; i++)
	{
	    int x,y,c=0;
	    scanf("%d%d", &x,&y);
	    if(x<y)
	    {
	        printf("%d", 0);
	    }
	   else{
	       c=x-y;
	        printf("%d\n", c);
	   }
	    
	}
	return 0;
}

